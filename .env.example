# Copiez en .env.local pour le dev local avec Vercel CLI (vercel dev)
# Sur Vercel : Project → Settings → Environment Variables

ADMIN_PIN=votre-code-secret
# SHEET_ID=1o9A924ybUdpSoM9AQ6nOeyuft6lytrgXG3pIKFLXvBk
# SHEET_NAME=Feuille 1
# BLOB_READ_WRITE_TOKEN=  → copier depuis Vercel Storage → Blob → onglet ".env.local"
# BLOB_ACCESS=public        → optionnel (défaut public) ; mettre "private" si store privé
# Sur Vercel, BLOB_STORE_ID + OIDC suffisent — évitez un token manuel périmé.
